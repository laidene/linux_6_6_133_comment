# dmb dsb isb

## dmb(`data mem barrier`)
保证当前核心的内存读写顺序.
ish = inner shareable
sy  = full system

## dsb(`data sync barrier`)
保证当前核心的读写以先做完

## isb(`instruction sync barrier`)
刷新流水线


ish 作用于 通常就是 CPU-CPU 同步 (`一个 cluster 内，或者几个紧密一致性的 cluster 内`)
cluster = 多个 CPU 核组成的硬件小组，通常共享 cache 并保持紧密一致性
sy 作用于 ish + 设备/MMIO/更广域观察者

nsh = Non-shareable

只要求对当前 PE/CPU 的非共享域生效
主要是“我这个核自己要看到正确顺序/完成”
不强调别的核何时观察到

# dc cisw | dc isw | dc csw

# dc civac