qemu入口处理

```asm
 0x0000000040000000  ? ldr      x0, 0x40000018	// 设置dtb地址
 0x0000000040000004  ? mov      x1, xzr		// 清空 x123
 0x0000000040000008  ? mov      x2, xzr
 0x000000004000000c  ? mov      x3, xzr
 0x0000000040000010  ? ldr      x4, 0x40000020 	// 物理地址0x40000020上的值是内核起始地址  查看内核地址 p /x *0x40000020
 0x0000000040000014  ? br       x4		// 跳转到内核	查看内核起始地址的指令 x/5i 0x40200000			x是查看内容  而/i是汇编    /x是查看16进制
```

反汇编
/home/c/my/board/qemu_linux/linux-6.6.133/vmlinux
aarch64-linux-gnu-objdump -d /home/c/my/board/qemu_linux/linux-6.6.133/vmlinux > vmlinux.disasm.S


arch/arm64/kernel/head.S
arch/arm64/kernel/vmlinux.lds.S

 停在内核入口
 b *0x40200000


 停在 primary_entry
 b *0x413800a0

b *0x41398d44
 record_mmu_state

指令集单步调试用 si


primary_entry
	record_mmu_state
	preserve_boot_args


>>> info reg x0
x0             0x41666000          1097228288


虚拟起始地址	_text			ffff800080000000
想要的起始地址	preserve_boot_args	ffff800081198d98	偏移	119 8D98	0x41398D98
					0xffff800081466000		146 6000
0xffff8000811800dc
0x413800DC
真是起始地址	0x40200000

想要的真实地址	0x41398D98


# 区别cpu屏障和编译器屏障

CPU 不是老式那种完全串行机器

它不是为了让代码“按文本顺序执行”，而是为了让“内存访问的可见顺序”符合你的要求。

最经典的例子：

data = 123;
flag = 1;
另一个核：

while (flag == 0) ;
printf("%d\n", data);
如果没有屏障，另一个核可能看到：

flag == 1
但 data 还是旧值
`因为 flag 那次写先对外可见了，data 还卡在 store buffer 或 cache 传播过程中。`


str data, [addr_data]
dmb ish
str flag, [addr_flag]
那含义是：

别的核如果已经看到了 flag = 1
就不能再看到旧的 data
也就是说，data 不一定要求在写端本核上“完全做完、完全退休”，但在体系结构保证下，它不能在“对外可见顺序”上落到 flag 后面。

# dsb
所以“有顺序”并不等于“已经完成”。

举个最直观的例子：

str x0, [addr]
dmb sy
wfi
dmb sy 只能保证：

如果后面还有别的内存访问，它们不能越过这次 str
但它不能保证：

这次 str 在执行 wfi 前已经真正发到总线/设备/可见点

所以这里可能出现：

写还在 store buffer 里
CPU 已经进 wfi 了
这时就要用：

str x0, [addr]
dsb sy
wfi
因为 dsb 会等这次写真的完成，再允


dmb：保证内存访问顺序
dsb：保证内存访问顺序，并且要求前面的内存访问/维护操作先完成，后面的指令才能继续

# ISB

指令a
dsb sy
指令b
到了 指令b 时，前面的内存操作确实已经完成了，但这仍然不能替代 isb。原因是：

dsb 管“完成”，不管“取指上下文切换”
如果 指令a 改了 SCTLR_EL1、TTBR_EL1、TCR_EL1、VBAR_EL1 这类会影响后续执行语义的寄存器，dsb 只能保证写寄存器这件事完成了，不能保证后面的指令已经按新状态重新取指。

后续指令可能已经在流水线里了
指令b 虽然还没正式执行，但可能已经被预取、译码，甚至带着旧的 MMU/权限/endianness/异常向量上下文准备好了。
isb 的作用就是把这些旧流水线内容清掉，让后面的指令按新环境重新来。

有些系统寄存器修改，架构上就要求 isb
典型模板就是：

asm

msr sctlr_el1, x0
isb
意思不是“等内存完成”，而是“从这之后，指令流按新的 SCTLR_EL1 状态执行”。

一个典型例子：

msr ttbr0_el1, x0
dsb sy
isb
这里：

dsb：确保相关内存/维护操作完成
isb：确保后面的取指和地址翻译使用新的页表上下文

b *0x41398D98
b *0x40231c9c __pi_dcache_inval_poc

disassemble /m __pi_dcache_inval_poc

# 查看pc之前的命令
x/10i $pc-12




